---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.15.0
  name: postgresqlserviceusers.postgresql.lunar.tech
spec:
  group: postgresql.lunar.tech
  names:
    kind: PostgreSQLServiceUser
    listKind: PostgreSQLServiceUserList
    plural: postgresqlserviceusers
    singular: postgresqlserviceuser
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PostgreSQLServiceUser is the Schema for the postgresqlserviceusers
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PostgreSQLServiceUserSpec defines the desired state of PostgreSQLServiceUser
            properties:
              host:
                description: Host to connect to
                properties:
                  value:
                    description: Defaults to "".
                    type: string
                  valueFrom:
                    description: Source to read the value from.
                    properties:
                      configMapKeyRef:
                        description: Selects a key of a config map in the custom resource's
                          namespace
                        properties:
                          key:
                            description: The key of the secret or config map to select
                              from.  Must be a valid key.
                            type: string
                          name:
                            description: The name of the secret or config map in the
                              namespace to select from.
                            type: string
                        required:
                        - key
                        type: object
                      secretKeyRef:
                        description: Selects a key of a secret in the custom resource's
                          namespace
                        properties:
                          key:
                            description: The key of the secret or config map to select
                              from.  Must be a valid key.
                            type: string
                          name:
                            description: The name of the secret or config map in the
                              namespace to select from.
                            type: string
                        required:
                        - key
                        type: object
                    type: object
                type: object
              name:
                description: Name of the service user
                type: string
              password:
                description: Password used with the Host and Name used to connect
                  to the database
                properties:
                  value:
                    description: Defaults to "".
                    type: string
                  valueFrom:
                    description: Source to read the value from.
                    properties:
                      configMapKeyRef:
                        description: Selects a key of a config map in the custom resource's
                          namespace
                        properties:
                          key:
                            description: The key of the secret or config map to select
                              from.  Must be a valid key.
                            type: string
                          name:
                            description: The name of the secret or config map in the
                              namespace to select from.
                            type: string
                        required:
                        - key
                        type: object
                      secretKeyRef:
                        description: Selects a key of a secret in the custom resource's
                          namespace
                        properties:
                          key:
                            description: The key of the secret or config map to select
                              from.  Must be a valid key.
                            type: string
                          name:
                            description: The name of the secret or config map in the
                              namespace to select from.
                            type: string
                        required:
                        - key
                        type: object
                    type: object
                type: object
              roles:
                description: Roles to grant to the select name
                items:
                  description: PostgreSQLServiceUserSpec defines the desired state
                    of PostgreSQLServiceUser
                  properties:
                    roleName:
                      description: RoleName is the name of the role to which to grant
                        to the user
                      type: string
                  required:
                  - roleName
                  type: object
                type: array
            required:
            - host
            - name
            type: object
          status:
            description: PostgreSQLServiceUserStatus defines the observed state of
              PostgreSQLServiceUser
            properties:
              error:
                description: Error if present, how did the reconciliation loop fail
                type: string
              host:
                description: Host, which host was reconciled
                type: string
              name:
                description: Name, which service user was reconciled
                type: string
              phase:
                description: Phase which state is the given resource currently in
                type: string
              phaseUpdated:
                description: PhaseUpdated when was the phase last updated
                format: date-time
                type: string
            required:
            - phase
            - phaseUpdated
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
